BusinessTransactionLog.allow({
	insert: function (userId, doc) {
		return false;
	},

	update: function (userId, doc, fields, modifier) {
		return false;
	},

	remove: function (userId, doc) {
		return false;
	}
});

BusinessTransactionLog.before.insert(function(userId, doc) {
	doc.createdAt = new Date();
	doc.createdBy = userId;
	doc.modifiedAt = doc.createdAt;
	doc.modifiedBy = doc.createdBy;

	
	if(!doc.createdBy) doc.createdBy = userId;
});

BusinessTransactionLog.before.update(function(userId, doc, fieldNames, modifier, options) {
	modifier.$set = modifier.$set || {};
	modifier.$set.modifiedAt = new Date();
	modifier.$set.modifiedBy = userId;

	
});

BusinessTransactionLog.before.upsert(function(userId, selector, modifier, options) {
	modifier.$set = modifier.$set || {};
	modifier.$set.modifiedAt = new Date();
	modifier.$set.modifiedBy = userId;

	/*BEFORE_UPSERT_CODE*/
});

BusinessTransactionLog.before.remove(function(userId, doc) {
	
});

BusinessTransactionLog.after.insert(function(userId, doc) {
	
});

BusinessTransactionLog.after.update(function(userId, doc, fieldNames, modifier, options) {
	
});

BusinessTransactionLog.after.remove(function(userId, doc) {
	
});
